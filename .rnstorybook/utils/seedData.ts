/**
 * Seed data utilities for Storybook stories.
 *
 * These hooks use @comapeo/core-react hooks to create test data
 * in the running backend. Pattern from:
 * src/frontend/screens/ComapeoSettings/CreateTestData.tsx
 *
 * Usage: Call these inside story play functions or wrapper components.
 * All stories share the same running backend, so data persists between stories.
 */

export {};
