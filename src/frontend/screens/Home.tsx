import * as React from 'react';
import { View } from 'react-native';

export const Home = () => {
    return(
        <View>
            {/* Add buttons here for all the screens */}
        </View>
    )
}